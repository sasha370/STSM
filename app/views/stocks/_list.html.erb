
<% if !@tracked_stocks.empty? %>
  <div class="row">
    <div class="col-9">

      <table class="table table-bordered table-hover">
        <thead>
        <tr>
          <th scope="col">Акция</th>
          <th scope="col">Организация</th>
          <th scope="col">Цена</th>
          <th scope="col" class="text-center">Действие</th>
        </tr>
        </thead>
        <tbody>
        <% @tracked_stocks.each do |stock| %>
          <tr>
            <th scope="row">
              <%= stock.ticker %></th>
            <td>
              <%= stock.name %></td>
            <td>
              <%= stock.last_price %></td>
            <td class="text-center">
<!--              В кнопку Удалить нам надо отправить по маршруту и передать текущую Акцию, т.к. в таблице UserStock он нам нужен -->
<!--              как и userID, который мы получим из current_user-->
              <%= link_to "Удалить", user_stock_path(stock), method: :delete, data: {confirm: "Вы уверены?"},
                                                                  class: "btn btn-sm btn-warning"%></td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
<% end %>
